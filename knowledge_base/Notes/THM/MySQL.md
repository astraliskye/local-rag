`show databases;` - list available databases
`use <db_name>;` - select a database
`show tables;` - list tables in the current database
# Functions
## String
`CONCAT()`
`GROUP_CONCAT()` - works even without group by clause
`SUBSTRING()`
`LENGTH()`
## Aggregate
`COUNT()`
`SUM()`
`MAX()`
`MIN()`
