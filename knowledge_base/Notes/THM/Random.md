# Extract rar
`unrar e -ppassword rarfile.rar`
# List .dll loaded by a Windows executable
tasklist /m /fi "pid eq 1304"
# Interesting Windows Locations
C:\Windows\System32\config
- Passwords n shit
Administrator documents folder and desktop
# Web Security Headers
Content-Security-Policy
Strict-Transport-Security
X-Content-Options
Referrer-Policy
# Python run OS commands
`print(os.popen('ls -l).read())`
